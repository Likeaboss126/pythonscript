for d in abc/*/; do
  [ -d "$d" ] || continue
  files=$(find "$d" -maxdepth 1 -type f | wc -l)
  dirs=$(find "$d" -maxdepth 1 -type d | wc -l)
  echo "Folder: ${d%/} | Files: $files | Folders: $((dirs - 1))"
done
